# stuff i ran to install all libs
pip install pdfminer sklearn nltk lexnlp
conda install tensorflow  # i think pip would work too

>>> import nltk
>>> nltk.download('punkt')
>>> nltk.download('averaged_perceptron_tagger')


# for the nodejs server
`npm i` in root directory
`npm start` to start the server 